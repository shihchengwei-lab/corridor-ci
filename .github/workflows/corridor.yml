name: Corridor CI

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  review-packet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./
        with:
          mode: fail
          source: body
          small_change_max_files: 1
          max_changed_files: 12
